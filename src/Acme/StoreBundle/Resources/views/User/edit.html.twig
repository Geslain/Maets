<h1>User edit</h1>


{{ form_start(edit_form, { "attr": {"action" : path('user_update', { 'id': document.id }), 'method':'post'}}) }}
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
    <li>
    {{ form_start(delete_form, { "attr": {"action" :  path('user_delete', { 'id': document.id }), 'method':'post'}}) }}
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
