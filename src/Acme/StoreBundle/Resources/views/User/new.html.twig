<h1>User creation</h1>

{{ form_start(form, { "attr": {"action" : path('user_create'), 'method':'post'}}) }}
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
</ul>
{{ form_end(form)}}